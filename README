
VERTEX PIPELINE UTILITY

NAME
    pipeline - commands to use and manipulate vertex pipelines
    
DEPENDENCIES
    python-paramiko
        Needs following modification to paramiko/pkey.py to be able to use keys generated by AWS:
            - from Crypto.Cipher import DES3
            + from Crypto.Cipher import DES3,AES
            ...
              _CIPHER_TABLE = {
		    +     'AES-128-CBC': { 'cipher': AES, 'keysize': 16, 'blocksize': 16, 'mode': AES.MODE_CBC },
                  'DES-EDE3-CBC': { 'cipher': DES3, 'keysize': 24, 'blocksize': 8, 'mode': DES3.MODE_CBC }
              }
    
SYNOPSIS
    python2.7 pipeline.py set|get ACCESS_KEY|SECRET_KEY <value>
        Set/get the value of an environment variable.
        
    python2.7 pipeline.py run <path> [-s <start>] [<arg 1>,<arg 2>,...]
        Run pipeline script located at path. The start option is an integer that sets the start point of the pipeline. Arg is an argument that will be passed to script.
