VERTEX PIPELINE UTILITY
	Allows usage of Amazon Web Services to run python scripts.

DEPENDENCIES
	fabric 	-	for ssh connections to ec2 instances
	
INSTALLATION
	This pipeline utility uses python's distutils. To install this package run:
		$ sudo python2.7 setup.py install
		
UNINSTALL
	To remove files associated with this utility, run the following commands:
		$ sudo rm /usr/local/bin/vertex_pipeline
		$ sudo rm /usr/local/lib/python2.7/dist-packages/vertex_pipeline-1.0.egg-info
		$ sudo rm -r /usr/local/lib/python2.7/dist-pacakges/pipeline/
	
NOTES
	Fabric will install python paramiko. The following following modification to paramiko/pkey.py must be made in order for pipelines to be able to use keys generated by AWS:
        - from Crypto.Cipher import DES3
        + from Crypto.Cipher import DES3,AES
        ...
          _CIPHER_TABLE = {
	    +     'AES-128-CBC': { 'cipher': AES, 'keysize': 16, 'blocksize': 16, 'mode': AES.MODE_CBC },
              'DES-EDE3-CBC': { 'cipher': DES3, 'keysize': 24, 'blocksize': 8, 'mode': DES3.MODE_CBC }
          }