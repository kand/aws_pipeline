VERTEX PIPELINE UTILITY

NAME
    pipeline - commands to use and manipulate vertex pipelines
    
SYNOPSIS
    python2.7 pipeline.py set|get ACCESS_KEY|SECRET_KEY <value>
        Set/get the value of an environment variable.
        
    python2.7 pipeline.py run [-e] <path> [-s <start> <result>] [<arg 1>,<arg 2>,...]
        Run pipeline script located at path. 
        
        -e 		run the script remotely on an ec2 instance.
        -s 		set the start stage of pipeline
        			start  = an integer start point for pipeline
        			result = path to file that contains results of previous stage
       	<arg n> argument to pass to the script
